# 阶段 2：TUI 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 做一个能在终端里日常使用的 Cinba 客户端，同时检验 `core-client` 的抽象是否真的跨前端复用。

**Architecture:** 单进程，无 IPC 边界。复用 `core-host`（起 Pi、自带权限门）与 `core-client` 的 `CoreClient` / `StdioTransport` / `createEventFolder`；**不用 `session.ts`，也不用 `foldUiRequest`**——那两样服务的是「视图与状态之间隔着边界」的架构，TUI 没有那道边界。界面用 pi-tui，全部代码集中在单个文件里。

**Tech Stack:** `@earendil-works/pi-tui` 0.85.1、Node 24 原生类型剥离（无构建步骤）。

设计依据：`docs/specs/2026-09-07-phase2-tui-design.md`

---

## 进度

| Task | 状态 |
|---|---|
| 1. 包骨架与 pi-tui 冒烟 | 未开始 |
| 2. 纯对话跑通 | 未开始 |
| 3. 工具显示与权限确认 | 未开始 |
| 4. 状态栏、中止、退出 | 未开始 |
| 5. 手工验收与收尾 | 未开始 |

---

## 动手前查实的事实

全部来自 `node_modules/@earendil-works/pi-coding-agent/node_modules/@earendil-works/pi-tui/dist/*.d.ts`
与官方示例 `node_modules/@earendil-works/pi-coding-agent/examples/rpc-extension-ui.ts`（641 行，做的事与本阶段几乎一致，可作参照）。

**API 形状**

- `new ProcessTerminal()` → `new TuiMainScreen(terminal)`，类型为 `TUI`
- `TUI`：`addChild(c)`、`setFocus(c | null)`、`start()`、`stop()`、`requestRender()`、`addInputListener(fn)`
- `Container`：`addChild(c)`、`clear()`
- `Component` 接口：必须实现 `render(width: number): string[]` 与 `invalidate(): void`；可选 `handleInput(data: string): void`
- `Input`：`onSubmit`、`onEscape`、`getValue()`、`setValue()`、`handleInput()`、`render()`、`invalidate()`
- **`SelectList(items, maxVisible, theme, layout?)` 的 `theme` 是必填的**，五个函数一个都不能少：
  `selectedPrefix` / `selectedText` / `description` / `scrollInfo` / `noMatch`。漏了会直接崩。
- `matchesKey(data, "ctrl+c")` 可用；`"ctrl+c"` 是合法的 KeyId
- `addInputListener` 的回调返回 `{ consume?: boolean; data?: string } | undefined`，在焦点路由**之前**执行

**两个必须知道的行为**

1. **终端处于 raw mode**（`ProcessTerminal` 调了 `setRawMode`），所以 **Ctrl+C 不会变成 SIGINT**，它作为普通输入数据送进来，必须自己处理。
2. **`TuiMainScreen` 渲染主屏并与上一帧做 diff**，只重画变化的区间。已经滚出可视区域的内容改不动——所以工具状态变化用**追加**而不是原地修改。追加式在终端里也更自然，它本来就是一份日志。

**一个坑（阶段 1b 踩过）**

`npm start --workspace <包>` 会把工作目录设成那个包的目录。TUI 的工作目录必须是**用户启动时所在的目录**，所以**不要**用 `--workspace` 启动它，直接 `node <仓库路径>/packages/tui/src/index.ts`。

**TUI owns the screen**

TUI 接管屏幕期间不能用 `console.log` / `console.error`，会把画面冲花。Pi 的 stderr 要写进输出区，不能直接打到终端。

---

## 文件结构

```
packages/tui/
├── package.json
└── src/index.ts
```

`index.ts` 内部的组织（单文件，约 230 行）：

```
ANSI 常量与 SelectList 主题
class Transcript    输出区。追加式，appendInline 用于流式文本
class StatusBar     底部一行：token、费用、当前可用按键
class PromptInput   标签 + Input
class ConfirmDialog 标题 + SelectList（是/否）
main()              起 Pi、组装界面、把动作画出来
```

单文件是有意的：本阶段重点是验证复用，不是搭第二套 UI 框架。真长到难读了再拆。

---

## Task 1：包骨架与 pi-tui 冒烟（拆雷）

**目的：** 在接入 Pi 之前，先确认 pi-tui 本身能用——能渲染、能收输入、Ctrl+C 能干净退出。
这一步不涉及任何 agent 逻辑，出问题范围明确。

**Files:**
- Create: `packages/tui/package.json`
- Create: `packages/tui/src/index.ts`

- [ ] **Step 1: 创建包**

`packages/tui/package.json`：

```json
{
  "name": "@cinba/tui",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "dependencies": {
    "@cinba/core-client": "*",
    "@cinba/core-host": "*",
    "@earendil-works/pi-tui": "^0.85.1"
  }
}
```

注意没有 `scripts.start`——见上文那个坑，这个程序不能用 `npm --workspace` 启动。

- [ ] **Step 2: 写冒烟版 index.ts**

`packages/tui/src/index.ts`：

```typescript
// 冒烟实验：验证 pi-tui 能渲染、能收键盘、Ctrl+C 能干净退出。
// 这一版不接 Pi。

import { Container, Input, matchesKey, ProcessTerminal, TuiMainScreen } from "@earendil-works/pi-tui";
import type { Component, TUI } from "@earendil-works/pi-tui";

const DIM = "\x1b[2m";
const BOLD = "\x1b[1m";
const GREEN = "\x1b[32m";
const RESET = "\x1b[0m";

class Transcript implements Component {
  #lines: string[] = [];

  append(line: string): void {
    this.#lines.push(line);
  }

  invalidate(): void {}

  render(width: number): string[] {
    return this.#lines.map((line) => line.slice(0, width));
  }
}

class PromptInput implements Component {
  readonly input = new Input();

  handleInput(data: string): void {
    this.input.handleInput(data);
  }

  invalidate(): void {
    this.input.invalidate();
  }

  render(width: number): string[] {
    return [`${GREEN}${BOLD}你：${RESET}`, ...this.input.render(width)];
  }
}

const terminal = new ProcessTerminal();
const tui: TUI = new TuiMainScreen(terminal);

const transcript = new Transcript();
const promptInput = new PromptInput();

const root = new Container();
root.addChild(transcript);
root.addChild(promptInput);
tui.addChild(root);
tui.setFocus(promptInput.input);

transcript.append(`${BOLD}pi-tui 冒烟实验${RESET}`);
transcript.append(`${DIM}打字后回车会回显。Ctrl+C 退出。${RESET}`);
transcript.append("");

promptInput.input.onSubmit = (value: string) => {
  const text = value.trim();
  promptInput.input.setValue("");
  if (text !== "") transcript.append(`你说：${text}`);
  tui.requestRender();
};

// 终端处于 raw mode，Ctrl+C 不会变成 SIGINT，必须自己截。
tui.addInputListener((data: string) => {
  if (matchesKey(data, "ctrl+c")) {
    tui.stop();
    process.exit(0);
  }
  return undefined;
});

tui.start();
```

- [ ] **Step 3: 安装**

```powershell
npm install
```

期望：`node_modules/@earendil-works/pi-tui` 出现在根目录（之前它只作为 Pi 的间接依赖存在于嵌套目录里）。

用这条确认：

```powershell
Test-Path node_modules\@earendil-works\pi-tui\dist\index.js
```

期望 `True`。

- [ ] **Step 4: 跑起来（关键验证）**

**必须在真实终端里跑**，不能在没有 TTY 的环境里跑。

```powershell
node packages\tui\src\index.ts
```

期望：

1. 屏幕上出现「pi-tui 冒烟实验」和提示行
2. 底部有一个「你：」输入框，打字能看见字
3. 回车后上方出现「你说：xxx」，输入框清空
4. **Ctrl+C 干净退出**，终端恢复正常（光标在，能继续敲命令）

第 4 点要留意：如果退出后终端变得不响应或者不回显，说明 raw mode 没恢复，需要在 `tui.stop()` 上排查。

- [ ] **Step 5: 提交**

```powershell
git add -A
git commit -m "feat(tui): pi-tui smoke test"
```

---

## Task 2：纯对话跑通

**Files:**
- Modify: `packages/tui/src/index.ts`（整份替换）

- [ ] **Step 1: 整份替换 index.ts**

```typescript
// Cinba 终端客户端。
//
// 与 Electron GUI 共用 core-host 与 core-client，只有「画出来」这一层不同。
// 用法：node <仓库路径>/packages/tui/src/index.ts
//
// 工作目录 = 启动时所在目录。Pi 的会话按工作目录隔离，所以 cd 到哪就在哪开工。
// 不要用 npm --workspace 启动——那会把工作目录设成包所在目录。

import { Container, Input, matchesKey, ProcessTerminal, TuiMainScreen } from "@earendil-works/pi-tui";
import type { Component, TUI } from "@earendil-works/pi-tui";
import { startCore } from "@cinba/core-host";
import { CoreClient, createEventFolder, StdioTransport } from "@cinba/core-client";
import type { ViewAction } from "@cinba/core-client";

const DIM = "\x1b[2m";
const BOLD = "\x1b[1m";
const GREEN = "\x1b[32m";
const BLUE = "\x1b[34m";
const RED = "\x1b[31m";
const RESET = "\x1b[0m";

/**
 * 输出区。追加式：画过的行不再改动。
 *
 * 为什么不做「原地更新」：TuiMainScreen 渲染主屏并与上一帧 diff，只有还在可视区域内的
 * 行能重画，滚上去的改不动。追加式在终端里也更自然——它本来就是一份日志。
 */
class Transcript implements Component {
  #lines: string[] = [];
  #max = 2000;

  append(line: string): void {
    this.#lines.push(line);
    if (this.#lines.length > this.#max) this.#lines = this.#lines.slice(-this.#max);
  }

  /** 接在最后一行末尾。流式文本靠这个一个字一个字长出来。 */
  appendInline(text: string): void {
    if (this.#lines.length === 0) this.#lines.push("");
    this.#lines[this.#lines.length - 1] += text;
  }

  invalidate(): void {}

  render(width: number): string[] {
    return this.#lines.map((line) => line.slice(0, width));
  }
}

class PromptInput implements Component {
  readonly input = new Input();

  handleInput(data: string): void {
    this.input.handleInput(data);
  }

  invalidate(): void {
    this.input.invalidate();
  }

  render(width: number): string[] {
    return [`${GREEN}${BOLD}你：${RESET}`, ...this.input.render(width)];
  }
}

// ---- 组装 ----

const terminal = new ProcessTerminal();
const tui: TUI = new TuiMainScreen(terminal);

const transcript = new Transcript();
const promptInput = new PromptInput();

const root = new Container();
root.addChild(transcript);
root.addChild(promptInput);
tui.addChild(root);
tui.setFocus(promptInput.input);

// ---- 起核心 ----

const child = startCore();

// TUI 占着屏幕，不能用 console.error，否则画面会被冲花。Pi 的报错写进输出区。
child.stderr?.setEncoding("utf8");
child.stderr?.on("data", (chunk: string) => {
  for (const line of chunk.trimEnd().split("\n")) {
    transcript.append(`${RED}[pi] ${line}${RESET}`);
  }
  tui.requestRender();
});

const client = new CoreClient(new StdioTransport(child));
const fold = createEventFolder();

let busy = false;

function applyAction(action: ViewAction): void {
  switch (action.type) {
    case "message_added":
      transcript.append("");
      transcript.append(
        action.role === "user" ? `${GREEN}${BOLD}你：${RESET}` : `${BLUE}${BOLD}助手：${RESET}`,
      );
      transcript.append(""); // 正文起始行，供 appendInline 往上接
      break;

    case "text_appended": {
      // 增量里可能带换行，得拆开：第一段接在当前行，其余各起一行。
      const parts = action.text.split("\n");
      transcript.appendInline(parts[0] ?? "");
      for (const part of parts.slice(1)) transcript.append(part);
      break;
    }

    case "busy_changed":
      busy = action.busy;
      break;

    // thinking 本阶段不显示；工具与费用在后续 task 接上。
    default:
      break;
  }
  tui.requestRender();
}

client.onEvent((event) => {
  for (const action of fold(event)) applyAction(action);
});

// 本阶段还没接权限确认，先一律拒绝，免得工具在无人看管下执行。
client.onUiRequest(async () => ({ confirmed: false }));

// ---- 交互 ----

promptInput.input.onSubmit = (value: string) => {
  // 回答期间不接受新输入，而且**不清空**——用户在流式输出期间打的字必须留着，
  // 否则「边看输出边打字」这个能力就废了一半。
  if (busy) return;
  const text = value.trim();
  if (text === "") return;
  promptInput.input.setValue("");
  void client.prompt(text);
};

function exit(): void {
  tui.stop();
  void client.close();
  process.exit(0);
}

tui.addInputListener((data: string) => {
  if (matchesKey(data, "ctrl+c")) exit();
  return undefined;
});

transcript.append(`${BOLD}Cinba${RESET} ${DIM}${process.cwd()}${RESET}`);
transcript.append(`${DIM}输入后回车发送，Ctrl+C 退出。${RESET}`);

tui.start();
```

- [ ] **Step 2: 验证纯对话**

在真实终端里：

```powershell
node packages\tui\src\index.ts
```

输入「说一个字：好」回车。期望：

1. 出现绿色的「你：」和你输入的内容
2. 出现蓝色的「助手：」，文字**逐字冒出来**
3. **输出期间继续打字，你打的字不被冲掉**（这是选 pi-tui 的全部理由，务必验）
4. Ctrl+C 干净退出

- [ ] **Step 3: 提交**

```powershell
git add -A
git commit -m "feat(tui): plain conversation over the shared core"
```

---

## Task 3：工具显示与权限确认

**Files:**
- Modify: `packages/tui/src/index.ts`

- [ ] **Step 1: 加 SelectList 主题与确认对话框**

在 `import` 区把 pi-tui 那一行替换为（多引入 `SelectList` 与它的主题类型）：

```typescript
import {
  Container,
  Input,
  matchesKey,
  ProcessTerminal,
  SelectList,
  TuiMainScreen,
} from "@earendil-works/pi-tui";
import type { Component, SelectListTheme, TUI } from "@earendil-works/pi-tui";
```

在 ANSI 常量后面加上主题与对话框（`SelectList` 的 theme 是必填的，五个函数一个都不能少）：

```typescript
const YELLOW = "\x1b[33m";
const MAGENTA = "\x1b[35m";

const SELECT_THEME: SelectListTheme = {
  selectedPrefix: (text) => `${MAGENTA}${text}${RESET}`,
  selectedText: (text) => `${MAGENTA}${text}${RESET}`,
  description: (text) => `${DIM}${text}${RESET}`,
  scrollInfo: (text) => `${DIM}${text}${RESET}`,
  noMatch: (text) => `${YELLOW}${text}${RESET}`,
};

/** 权限确认。确认期间它临时顶替底部的输入框。 */
class ConfirmDialog implements Component {
  #list: SelectList;
  #title: string;
  onAnswer?: (confirmed: boolean) => void;

  constructor(title: string) {
    this.#title = title;
    this.#list = new SelectList(
      [
        { value: "yes", label: "允许执行" },
        { value: "no", label: "拒绝" },
      ],
      2,
      SELECT_THEME,
    );
    this.#list.onSelect = (item) => this.onAnswer?.(item.value === "yes");
    // Esc 取消等同于拒绝——默认从严。
    this.#list.onCancel = () => this.onAnswer?.(false);
  }

  handleInput(data: string): void {
    this.#list.handleInput(data);
  }

  invalidate(): void {
    this.#list.invalidate();
  }

  render(width: number): string[] {
    return [
      `${YELLOW}${BOLD}${this.#title}${RESET}`,
      ...this.#list.render(width),
      `${DIM}↑↓ 选择，Enter 确认，Esc 拒绝${RESET}`,
    ];
  }
}
```

- [ ] **Step 2: 加底部区域切换**

在 `tui.setFocus(promptInput.input);` 那一行**下面**插入：

```typescript
/** 底部要么是输入框，要么是确认对话框。切换时整个重组一次。 */
function setBottom(component: Component): void {
  root.clear();
  root.addChild(transcript);
  root.addChild(component);
  tui.requestRender();
}

function showPrompt(): void {
  setBottom(promptInput);
  tui.setFocus(promptInput.input);
}

/** 弹出确认，等用户选完再 resolve。核心此刻正阻塞等着这个答案。 */
function ask(title: string): Promise<boolean> {
  return new Promise((resolve) => {
    const dialog = new ConfirmDialog(title);
    dialog.onAnswer = (confirmed) => {
      transcript.append(confirmed ? `${DIM}   → 已允许${RESET}` : `${DIM}   → 已拒绝${RESET}`);
      showPrompt();
      resolve(confirmed);
    };
    setBottom(dialog);
    tui.setFocus(dialog);
  });
}
```

- [ ] **Step 3: 把工具动作画出来**

在 `applyAction` 的 `switch` 里，`case "busy_changed"` **之前**插入：

```typescript
    case "tool_changed": {
      if (action.status === "pending") {
        transcript.append("");
        transcript.append(`${YELLOW}🔧 ${action.toolName}${RESET} ${DIM}待批准${RESET}`);
        if (action.args !== undefined) {
          for (const line of JSON.stringify(action.args, null, 2).split("\n")) {
            transcript.append(`${DIM}   ${line}${RESET}`);
          }
        }
      } else if (action.status === "done" || action.status === "error") {
        transcript.append(
          action.status === "done"
            ? `   ${GREEN}✅ 完成${RESET}`
            : `   ${RED}❌ 被拒绝或出错${RESET}`,
        );
        // 工具输出可能很长，截前 20 行——终端里刷屏比信息少更难受。
        for (const line of (action.result ?? "").split("\n").slice(0, 20)) {
          transcript.append(`${DIM}   ${line}${RESET}`);
        }
      }
      // running 不画：确认框消失本身就是「开始执行了」的信号。
      break;
    }
```

- [ ] **Step 4: 接上真的权限确认**

把 Task 2 里那句临时的一律拒绝：

```typescript
client.onUiRequest(async () => ({ confirmed: false }));
```

替换为：

```typescript
client.onUiRequest(async (request) => {
  // 只有 confirm 需要回话；notify 之类是广播式的，本阶段不显示。
  if (request.method !== "confirm") return { cancelled: true };
  const confirmed = await ask(String(request.title ?? "允许执行这个工具？"));
  return { confirmed };
});
```

注意这里**没有用 `foldUiRequest`**。那个函数的用处是把 UI 请求变成一个能跨边界传输的动作
（GUI 里要过 IPC）。TUI 是单进程，直接拿 `request` 用即可——这与不使用 `session.ts` 是同一个理由。

- [ ] **Step 5: 验证放行**

```powershell
node packages\tui\src\index.ts
```

输入「运行 ls 命令，告诉我当前目录下有什么」。期望：

1. 出现黄色的 `🔧 bash 待批准` 和命令参数
2. **底部输入框变成一个两项的选择列表**，程序停在这里等你
3. 选「允许执行」回车 → 出现 `✅ 完成` 和 ls 的输出 → 助手接着回答
4. 底部恢复成输入框

- [ ] **Step 6: 验证拦截（本阶段最关键的一步）**

再问一次同样的问题，这次选「拒绝」。期望：

1. 出现 `❌ 被拒绝或出错`，下面是「用户拒绝了这次工具调用」
2. **助手明确表示知道被拒绝了**，而不是假装执行成功

第 2 点证明拦截结果确实回到了模型那里——权限门在第三个前端上依然有效。

- [ ] **Step 7: 提交**

```powershell
git add -A
git commit -m "feat(tui): tool output and permission confirmation"
```

---

## Task 4：状态栏、中止、退出

**Files:**
- Modify: `packages/tui/src/index.ts`

- [ ] **Step 1: 加状态栏组件**

在 `class PromptInput` **后面**插入：

```typescript
/** 底部一行：累计用量与当前可用的按键。 */
class StatusBar implements Component {
  totalTokens = 0;
  totalCost = 0;
  busy = false;

  invalidate(): void {}

  render(_width: number): string[] {
    const usage = `${this.totalTokens} tokens · $${this.totalCost.toFixed(4)}`;
    const hint = this.busy ? "Esc 中止" : "Ctrl+C 退出";
    return [`${DIM}${usage}    ${hint}${RESET}`];
  }
}
```

- [ ] **Step 2: 把状态栏装进界面**

在 `const promptInput = new PromptInput();` 下面加一行：

```typescript
const statusBar = new StatusBar();
```

把 `root` 的组装改成（多一个 `statusBar`）：

```typescript
const root = new Container();
root.addChild(transcript);
root.addChild(promptInput);
root.addChild(statusBar);
tui.addChild(root);
tui.setFocus(promptInput.input);
```

并把 `setBottom` 改成也带上状态栏：

```typescript
function setBottom(component: Component): void {
  root.clear();
  root.addChild(transcript);
  root.addChild(component);
  root.addChild(statusBar);
  tui.requestRender();
}
```

- [ ] **Step 3: 让费用与忙碌状态进状态栏**

把 `applyAction` 里的 `case "busy_changed"` 替换为：

```typescript
    case "usage_changed":
      statusBar.totalTokens = action.totalTokens;
      statusBar.totalCost = action.totalCost;
      break;

    case "busy_changed":
      busy = action.busy;
      statusBar.busy = action.busy;
      break;
```

- [ ] **Step 4: 加 Esc 中止**

在 `promptInput.input.onSubmit = ...` 那一段**后面**插入：

```typescript
// Esc 中止进行中的回答。不忙的时候按它没有副作用——退出用 Ctrl+C，
// 免得手滑一下就把会话关了。
promptInput.input.onEscape = () => {
  if (!busy) return;
  void client.abort();
  transcript.append(`${YELLOW}[已中止]${RESET}`);
  tui.requestRender();
};
```

- [ ] **Step 5: 验证**

```powershell
node packages\tui\src\index.ts
```

1. 问一句话，看**底部状态栏**的 token 与费用有没有增长
2. 回答期间状态栏右侧应显示「Esc 中止」，空闲时显示「Ctrl+C 退出」
3. 问「写一首 20 行的诗」，输出到一半按 **Esc**，期望停下来并出现 `[已中止]`
4. 中止后**还能继续对话**（再问一句，正常回答）
5. Ctrl+C 退出

- [ ] **Step 6: 验证没有孤儿进程**

退出后：

```powershell
Get-CimInstance Win32_Process -Filter "Name='node.exe'" | Where-Object { $_.CommandLine -like '*rpc-entry*' } | Measure-Object | Select-Object Count
```

期望 `Count : 0`。

若有残留，检查 `exit()` 里的 `client.close()` 是否在 `process.exit(0)` 之前真的执行了。

- [ ] **Step 7: 提交**

```powershell
git add -A
git commit -m "feat(tui): status bar, abort, and clean exit"
```

---

## Task 5：手工验收与收尾

**Files:**
- Modify: `docs/plans/2026-09-07-phase2-tui.md`（进度表）
- Modify: `docs/specs/2026-09-07-phase2-tui-design.md`（第 6 节清单）

- [ ] **Step 1: 确认没破坏既有测试**

```powershell
node --test "packages/core-host/src/*.test.ts" "packages/core-client/src/*.test.ts"
```

期望：34 个全部 pass。本阶段不新增测试——该测的纯逻辑在阶段 1b 已经覆盖。

- [ ] **Step 2: 确认 GUI 仍然正常**

TUI 和 GUI 共用 `core-host` 与 `core-client`，本阶段虽然没改它们，仍应确认一次。

```powershell
npm start --workspace @cinba/desktop
```

问一句话，确认能正常回答，然后关掉窗口。

- [ ] **Step 3: 走完整验收清单**

在**仓库以外的某个目录**启动，用来验证工作目录那一条：

```powershell
cd C:\Users\shenxichen
node C:\Users\shenxichen\repos\Cinba\packages\tui\src\index.ts
```

- [ ] 连续多轮对话，输出逐字流式显示
- [ ] 模型输出期间打字，输入不被冲掉
- [ ] 触发 bash 时停下来问，选「允许执行」执行、选「拒绝」不执行且模型知道被拒
- [ ] Esc 能中止进行中的回答，中止后仍可继续对话
- [ ] Ctrl+C 退出后无残留 node 进程
- [ ] 状态栏的 token 与费用随对话增长
- [ ] 让它 `ls`，列出的是 `C:\Users\shenxichen` 的内容而不是仓库的内容

最后一条验证工作目录确实是启动时所在的目录。

- [ ] **Step 4: 记录架构结论**

在 `docs/specs/2026-09-07-phase2-tui-design.md` 第 4.1 节末尾追加实测结论，形如：

```markdown
**实测结论（2026-09-07）：** TUI 全程未使用 `session.ts` 与 `foldUiRequest`，
`createEventFolder` 原样复用。第 4.1 节的判断成立。
```

若实施中发现 TUI 其实需要账本，则如实记录相反的结论并说明原因——设计文档第 4.1 节
已经写明这条判断可能被推翻。

- [ ] **Step 5: 更新进度表并提交**

把本文件开头的进度表全部标为完成，并勾上设计文档第 6 节的清单。

```powershell
git add -A
git commit -m "docs: mark phase 2 complete"
```

---

## 阶段 2 完成标准

- [ ] Task 5 Step 3 的七项手工验收全部通过
- [ ] `node --test` 的 34 个测试仍全绿
- [ ] GUI 未受影响
- [ ] `pi-tui` 只出现在 `packages/tui/src/index.ts` 一个文件里
- [ ] 第 4.1 节关于 `session.ts` 的判断已被实测确认或修正

达成后进阶段 3（换 WebSocket，核心跑服务器）。届时主设计文档第 8 节记录的两条待解问题需要正式处理。
